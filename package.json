{
  "name": "mapbox-photo-tracking",
  "version": "1.0.1",
  "description": "Mapbox photo tracking app with Gemini API and OpenCV",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm run install-opencv",
    "install-opencv": "cmake-js compile"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.7",
    "opencv4nodejs": "^5.2.0"
  },
  "devDependencies": {
    "cmake-js": "^7.2.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
